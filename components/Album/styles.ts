import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: 200,
  },
  image: {
    width: "100%",
    height: 200,
  },
  title: {
    color: "grey",
    padding: 10,
  },
});

export default styles;
